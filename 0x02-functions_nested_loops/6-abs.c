#include "holberton.h"

/**
 * _abs - Entry point
 *
 * Description: Show a message blablabla
 * Return: Always 0 (Success)
 */

int _abs(int x)
{
	if (x >= 0)
		return (x);
		else
			return (-x);

}
